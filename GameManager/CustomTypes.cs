using AnyRPG;
﻿
public enum CombatTextType { normal, ability, gainHealth, gainXP, levelUp, gainMana, gainBuff, loseBuff }

public enum CombatMagnitude { normal, critical }

public enum DamageType { ability, physical }
